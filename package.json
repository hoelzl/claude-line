{
  "name": "claude-line",
  "version": "0.0.1",
  "description": "Voice-first mobile interface for driving Claude Code from a phone/tablet",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "vitest run",
    "generate-cert": "node src/generate-cert.js",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,json,md,html}": "prettier --write"
  },
  "author": "Dr. Matthias Hölzl <tc@xantira.com>",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1",
    "dotenv": "^16",
    "express": "^5",
    "form-data": "^4",
    "selfsigned": "^5.5.0",
    "ws": "^8"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1",
    "vitest": "^3"
  }
}
